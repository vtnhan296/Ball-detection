ultralytics
torch
opencv-python
numpy
scikit-learn
yacs
albumentations<2.0
scikit-image
Cython
h5py
tb-nightly
future
gdown
flake8
yapf
isort
tabulate
deepdiff
wandb
monai
torchmetrics==0.10.3

# If Microsoft C++ Build Tools is available, this can be installed directly.
# On Windows without build tools, clone the source to models/reid/prtreid_src
# and the classifier will use it as a local fallback.
# prtreid @ git+https://github.com/VlSomers/prtreid
